// TODO: Make Blocks
	
	// TODO: protection against the same block coming up more than twice in a row

int block[]					= {0,0,0,0,0,0}, // Block data: {xPos, yPos, blockType, rotation}
		xPos						= 0,
		yPos						= 0,
		blockWidth			= 0,
		blockHeight			= 0,
		blockTypes			= 0, // Block blockTypes: square = 0, T = 1, L = 2, J = 3, I = 4, S = 5, Z = 6
		rotation				= 0, // Rotation in degrees, can be 0, 90, 180, 270
		blockSize				= 20; // Relates to grid size

// Block Colors


// TODO: Create next block
int nextBlock() {

}
